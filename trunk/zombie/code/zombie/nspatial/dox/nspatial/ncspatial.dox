
//---------------------------------------------------------------------------------
/**
    @component Spatial
    @cppclass ncSpatial
    @superclass nEntityObject
    @classinfo
      Represents an element in the spatial system.
*/

//--------------------------------------------------------------------------------
/**
    @property BBox
    @format
      fff(Center)  -- Center of the bounding box
      fff(Extents) -- Extents of the bounding box
    @attrs (private=true)
    @info
      Spatial element's bounding box
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetBBox
    @cmdtype setter
    @property BBox
    @info 
      Set axis-aligned bounding box
*/

//--------------------------------------------------------------------------------
/**
    @cmd GetBBox
    @cmdtype getter
    @property BBox
    @info 
      Get axis-aligned bounding box
*/

//--------------------------------------------------------------------------------
/**
    @property OriginalBBox
    @format
      fff(Center)  -- Center of the original bounding box (for an instance)
      fff(Extents) -- Extents of the original bounding box (for an instance)
    @attrs (private=true)
    @info
      Spatial element's original bounding box (for an instance)
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetOriginalBBox
    @cmdtype setter
    @property OriginalBBox
    @info 
      Set original bbox (for an instance)
*/

//--------------------------------------------------------------------------------
/**
    @cmd GetOriginalBBox
    @cmdtype getter
    @property OriginalBBox
    @info 
      Get original bbox (for an instance)
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetCellId
    @input
      i(CellId) -- Spatial cell id where is the entity
    @info 
      Set the cell id the entity is in
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetSpaceId
    @input
      i(SpaceId) -- Spatial space id where is the entity
    @info 
      Set the space entity id this entity is in
*/

//--------------------------------------------------------------------------------
/**
    @property ModelType
    @format
      i(ModelType) -- Type of the test model
    @attrs (private=true)
    @info
      Spatial element's model type
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetModelType
    @cmdtype setter
    @property ModelType
    @info 
      Set the type of test model
*/

//--------------------------------------------------------------------------------
/**
    @cmd GetModelType
    @cmdtype getter
    @property ModelType
    @info 
      Get the type of test model
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetSphereProperties
    @input
     f(xpos)   -- X coordinate of the sphere's center
     f(ypos)   -- Y coordinate of the sphere's center
     f(zpos)   -- Z coordinate of the sphere's center
     f(radius) -- sphere's radius
    @info 
      Set the sphere model's properties
*/

//--------------------------------------------------------------------------------
/**
    @property Global
    @format
      b(Global) -- Says if the entity is global or not
    @attrs (private=true)
    @info
      Says if the entity is global or not
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetGlobal
    @cmdtype setter
    @property Global
    @info 
      Set the entity as global or not
*/

//--------------------------------------------------------------------------------
/**
    @cmd GetGlobal
    @cmdtype getter
    @property Global
    @info 
      Get if the entity is global or not
*/

//--------------------------------------------------------------------------------
/**
    @property DetermineSpace
    @format
      i(DetermineSpaceFlag; min=0;max=2;enums= YES:0,NO:1,NOTINIT:2) -- Block or not the cell determination when the entity moves
    @attrs (private=true)
    @info
      Block or not the cell determination when the entity moves
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetDetermineSpaceFlag
    @cmdtype setter
    @property DetermineSpace
    @info 
      Set the determine space flag
*/

//--------------------------------------------------------------------------------
/**
    @cmd GetDetermineSpaceFlag
    @cmdtype getter
    @property DetermineSpace
    @info 
      Get the determine space flag
*/

//--------------------------------------------------------------------------------
