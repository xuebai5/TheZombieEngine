#include "precompiled/pchnphysics.h"

#ifndef NGAME

const vector4 phy_color_boundingbox( 0, 1, 0, 1 );

const vector4 phy_color_boundingbox_selected( 1, 1, 1, 1 );

const vector4 phy_color_boundingbox_disabled( 0, 0.5, 0, 0.5 );

const vector4 phy_color_shape( 1, 0, 0, 0.5 );

const vector4 phy_color_normal_contact( 1, 1, 0, 1 );

const vector4 phy_color_ray( 0, 0, 1, 1 );

const vector4 phy_color_trianglemesh( 1, 0, 0, 1 );

const vector4 phy_color_trianglemesh_disabled( 0.5, 0.5, 0, 1 );

const vector4 phy_color_physicsobj_boundingbox( .5,.5,.5,1 );

const vector4 phy_color_areamanager( 0, 1, 1, 1 );

const vector4 phy_color_heightmap( 1, 0, 0, 1 );

const vector4 phy_color_heightmap_disabled( 0.5, 0, 0, 1 );

const vector4 phy_color_space( 1, 0, 1, 1 );

const vector4 phy_color_space_disabled( .5, 0, 0.5, 1 );

const vector4 phy_color_terrain_hole( 0,0,0,1 );

const vector4 phy_color_world_space( 1,1,0,1 );

const vector4 phy_color_dynamic_space( 0, 1, 1, 1 );

const vector4 phy_color_dynamic_space_disabled( 0, .5, .5, 1 );

const vector4 phy_color_normals( 1,1,0,1 );

const float phy_normals_lenght(.1f);

// joints colors
const vector4 phy_color_joint_axisA( 1, 0, 0, 1 );
const vector4 phy_color_joint_axisB( 0, 1, 0, 1 );
const vector4 phy_color_joint_axisC( 0, 0, 1, 1 );

const vector4 phy_color_joint_anchorA( 1, 0, 0, 1 );
const vector4 phy_color_joint_anchorB( 0, 1, 0, 1 );
const vector4 phy_color_joint_anchorC( 0, 0, 1, 1 );

const vector4 phy_color_joint_anchorWall( 1, 1, 0, 1 );

// joints anchor points radius
const float phy_radius_joint_anchor(0.01f);

// joints axis length
const float phy_length_joint_axis(0.1f);

#endif

//-----------------------------------------------------------------------------
// EOF
//-----------------------------------------------------------------------------
