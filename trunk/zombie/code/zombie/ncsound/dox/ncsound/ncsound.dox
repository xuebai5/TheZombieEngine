
//---------------------------------------------------------------------------------
/**
    @component Sound
    @cppclass ncSound
    @superclass nEntityObject
    @classinfo
      No information
*/
//--------------------------------------------------------------------------------
/**
    @property SoundDefaultVolume
    @format
      f(SoundDefaultVolume;min=0;max=1;step=0.01;editor=slider) -- Default volume for all sounds of this entity
    @attrs
    @info
      Set volume setting (default modulating volume for all sounds)
*/
//--------------------------------------------------------------------------------
/**
    @cmd Play
    @output
      v
    @input
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound
*/
//--------------------------------------------------------------------------------
/**
    @cmd PlayMaterial
    @output
      v
    @input
      s(unnamed) -- No info
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound given a material
*/
//--------------------------------------------------------------------------------
/**
    @cmd PlayAlone
    @output
      v
    @input
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound without attaching it to an entity
*/
//--------------------------------------------------------------------------------
/**
    @cmd PlayAloneMaterial
    @output
      v
    @input
      s(unnamed) -- No info
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound given a material without attaching it to an entity
*/
//--------------------------------------------------------------------------------
/**
    @cmd StopSound
    @output
      v
    @input
      v
    @info 
      Stop last sound
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetSoundVolume
    @cmdtype setter
    @property SoundVolume
    @info 
      Set volume
*/
//--------------------------------------------------------------------------------
/**
    @cmd GetSoundVolume
    @cmdtype getter
    @property SoundVolume
    @info 
      Get volume
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetSoundFrequency
    @cmdtype setter
    @property SoundFrequency
    @info 
      Set frequency
*/
//--------------------------------------------------------------------------------
/**
    @cmd GetSoundFrequency
    @cmdtype getter
    @property SoundFrequency
    @info 
      Get frequency
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetSoundDefaultVolume
    @cmdtype setter
    @property SoundDefaultVolume
    @info 
      Set volume setting (default modulating volume for all sounds)
*/
//--------------------------------------------------------------------------------
/**
    @cmd GetSoundDefaultVolume
    @cmdtype getter
    @property SoundDefaultVolume
    @info 
      Get volume setting (default modulating volume for all sounds)
*/
//--------------------------------------------------------------------------------
/**
    @cmd EnableSoundTrigger
    @output
      v
    @input
      v
    @info 
      Enable sound trigger
*/
//--------------------------------------------------------------------------------
/**
    @cmd DisableSoundTrigger
    @output
      v
    @input
      v
    @info 
      Disable sound trigger
*/
//--------------------------------------------------------------------------------
/**
    @cmd SynchronizeTriggerShape
    @output
      v
    @input
      v
    @info 
      Synchronize the trigger shape radius with the greatest maximum distance among all the table's sounds
*/
//--------------------------------------------------------------------------------
/**
    @cmd SendEventToPlay
    @output
      v
    @input
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound on all the clients
*/
//--------------------------------------------------------------------------------
/**
    @cmd PlaySoundEvent
    @output
      v
    @input
      s(unnamed) -- No info
      i(unnamed) -- No info
    @info 
      Play a sound on all the clients (nicer name to be used by designers)
*/
//--------------------------------------------------------------------------------
/**
    @cmd SendStopSound
    @output
      v
    @input
      v
    @info 
      Stop playing sound on all the clients
*/
//--------------------------------------------------------------------------------
/**
    @cmd StopLastSound
    @output
      v
    @input
      v
    @info 
      Stop playing sound on all the clients (nicer name to be used by designers)
*/
