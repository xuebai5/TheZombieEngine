#include "entity_test/entity01.h"

nNebulaEntityClass(nEntityClass1,"nentityclass")

/*
static void
n_printa(void* slf, nCmd* cmd)
{
    nComponentClassA* self = (nEntityClass1*) slf;
    self->printa();
}

static void
n_printb(void* slf, nCmd* cmd)
{
    nComponentClassB* self = (nEntityClass1*) slf;
    self->printb();
}

static void
n_printc(void* slf, nCmd* cmd)
{
    nComponentClassC* self = (nEntityClass1*) slf;
    self->printc();
}

void n_initcmds_nEntityClass1(nClass * cl)
{
    cl->BeginCmds();
    cl->AddCmd("v_printnodea_v", 'PRIA', n_printa);
    cl->AddCmd("v_printnodeb_v", 'PRIB', n_printb);
    cl->AddCmd("v_printnodec_v", 'PRIC', n_printc);
    cl->EndCmds();
}
*/