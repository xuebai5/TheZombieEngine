
//---------------------------------------------------------------------------------
/**
    @scriptclass ngamematerialserver
    @cppclass nGameMaterialServer
    @superclass nRoot
    @classinfo
      Represents a game material container and manager.
*/
//--------------------------------------------------------------------------------
/**
    @cmd GetMaterialByName
    @output
      v
    @input
      s(unnamed) -- No info
    @info 
      returns a material type trough the name
*/
//--------------------------------------------------------------------------------
/**
    @cmd CreateMaterial
    @output
      v
    @input
      s(unnamed) -- No info
    @info 
      creates a material
*/
//--------------------------------------------------------------------------------
/**
    @cmd Add
    @output
      v
    @input
      v
    @info 
      adds an already existing material
*/
//--------------------------------------------------------------------------------
/**
    @cmd Save
    @output
      v
    @input
      v
    @info 
      No information
*/
