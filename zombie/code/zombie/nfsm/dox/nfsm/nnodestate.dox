
//---------------------------------------------------------------------------------
/**
    @scriptclass nnodestate
    @cppclass nNodeState
    @superclass nState
    @classinfo
      State that contains a child FSM.
*/
//--------------------------------------------------------------------------------
/**
    @property FSMSelector
    @format
      o(FSM selector) -- No info
    @attrs
    @info
      Set the FSM selector used to select children FSMs (replaces the previous one, but does NOT release it)
*/
//--------------------------------------------------------------------------------
/**
    @property FSMSelectorByName
    @format
      s(FSM selector) -- FSM selector name
    @attrs
    @info
      Set the FSM selector giving its name (used for persistence)
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetFSMSelector
    @cmdtype setter
    @property FSMSelector
    @info 
      Set the FSM selector used to select children FSMs (replaces the previous one, but does NOT release it)
*/
//--------------------------------------------------------------------------------
/**
    @cmd GetFSMSelector
    @cmdtype getter
    @property FSMSelector
    @info 
      Get the FSM selector used to select children FSMs, or NULL if there isn't any
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetFSMSelectorByName
    @cmdtype setter
    @property FSMSelectorByName
    @info 
      Set the FSM selector giving its name (used for persistence)
*/
