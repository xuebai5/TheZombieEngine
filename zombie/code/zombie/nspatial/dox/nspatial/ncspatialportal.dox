
//---------------------------------------------------------------------------------
/**
    @component Spatial
    @cppclass ncSpatialPortal
    @superclass ncSpatial
    @classinfo
      Spatial portal component for portal entities.
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetOtherSideCellId
    @output
      v
    @input 
      i(CellId) -- Other side cell id 
    @info 
      Set the other side cell id
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetClipVertex
    @output
      v
    @input 
      i(VertexIndex)            -- Vertex index
      fff(VertexCoordinates)    -- Vertex coordinates
    @info 
      Set a clip rectangle's vertex
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetOriginalPlane
    @output
      v
    @input 
      ffff(ABCD) -- Plane parameters
    @info 
      Set the portal's original plane
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetOrigOBBoxSize
    @output
      v
    @input 
      fff(Size) -- Original oriented bounding box's size
    @info 
      Set the original oriented bounding box's size
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetOrigOBBoxScale
    @output
      v
    @input 
      fff(Scale) -- Original oriented bounding box's scale
    @info 
      Set the original oriented bounding box's scale
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetOrigOBBoxEuler
    @output
      v
    @input 
      fff(Euler) -- Original oriented bounding box's euler
    @info 
      Set the original oriented bounding box's euler
*/
//--------------------------------------------------------------------------------
/**
    @cmd SetOrigOBBoxTranslation
    @output
      v
    @input 
      fff(Translation) -- Original oriented bounding box's translation
    @info 
      Set the original oriented bounding box's translation
*/
//--------------------------------------------------------------------------------
/**
    @cmd Activate
    @output
      v
    @input
      v
    @info 
      Activate the portal
*/
//--------------------------------------------------------------------------------
/**
    @cmd Deactivate
    @output
      v
    @input
      v
    @info 
      Deactivate the portal
*/

//--------------------------------------------------------------------------------
/**
    @property ActiveFlag
    @format
      b(Flag) -- True if the portal is active, false otherwise
    @attrs (private=true)
    @info
      Portal's active flag
*/

//--------------------------------------------------------------------------------
/**
    @cmd SetActive
    @cmdtype setter
    @property ActiveFlag
    @info 
      Set the active flag
*/
//--------------------------------------------------------------------------------
/**
    @cmd IsActive
    @cmdtype getter
    @property ActiveFlag
    @info 
      Is the portal active?
*/
